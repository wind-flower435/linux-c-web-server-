project(sub_timer)

add_library(${PROJECT_NAME} timer.cpp)
add_library(sub_timer::timer ALIAS ${PROJECT_NAME})

target_include_directories(${PROJECT_NAME}
    PUBLIC 
        ${CMAKE_PROJECT_NAME}/misc
        ${CMAKE_PROJECT_NAME}/log
)

target_link_libraries( ${PROJECT_NAME}
    PRIVATE 
        sub_log::log
)